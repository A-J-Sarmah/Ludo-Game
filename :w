import pygame

class Player(pygame.sprite.Sprite):
    def __init__(self,color,image,x_position,y_position):
        super().__init__()
        self.image = image
        self.rect = self.image.get_rect(topleft=(x_position,y_position))
        self.color = color
